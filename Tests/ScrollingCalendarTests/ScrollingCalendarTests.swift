import XCTest
@testable import ScrollingCalendar

final class ScrollingCalendarTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(ScrollingCalendar().text, "Hello, World!")
    }
}
